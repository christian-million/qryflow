% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qryflow_execute.R
\name{qryflow_execute}
\alias{qryflow_execute}
\title{Execute a parsed qryflow SQL workflow}
\usage{
qryflow_execute(con, x, ..., on_error = c("stop", "continue"), verbose = TRUE)
}
\arguments{
\item{con}{A database connection from \code{\link[DBI:dbConnect]{DBI::dbConnect()}}}

\item{x}{A \code{qryflow} object, typically created by \code{\link[=qryflow_parse]{qryflow_parse()}}}

\item{...}{Reserved for future use}

\item{on_error}{Controls behaviour when a chunk fails during execution.
One of \code{"stop"} (default) or \code{"continue"}. \code{"stop"} halts
execution immediately and returns the partially executed workflow. \code{"continue"}
records the error in the chunk's \code{meta} and continues. The global
default can be set with \code{options(qryflow.on_error = "continue")}.}

\item{verbose}{Logical. If \code{TRUE}, emits a message before each chunk
identifying its name and type, and prints a summary on completion
reporting total chunks run, successes, errors, skipped, and elapsed time.
Defaults to \code{FALSE}. The global default can be set with
\code{options(qryflow.verbose = TRUE)}.}
}
\value{
An object of class \code{qryflow}, containing executed chunks with results and a \code{meta} field
that includes timing and source information.
}
\description{
\code{qryflow_execute()} takes a \code{qryflow} object (as returned by \code{\link[=qryflow_parse]{qryflow_parse()}}),
executes each chunk (e.g., \verb{@query}, \verb{@exec}), and collects the results and timing metadata.

This function is used internally by \code{\link[=qryflow_run]{qryflow_run()}}, but can be called directly in concert with \code{\link[=qryflow_parse]{qryflow_parse()}} if you want
to manually control parsing and execution.
}
\examples{
con <- example_db_connect(mtcars)

filepath <- example_sql_path("mtcars.sql")

parsed <- qryflow_parse(filepath)

executed <- qryflow_execute(con, parsed)

DBI::dbDisconnect(con)
}
\seealso{
\code{\link[=qryflow_run]{qryflow_run()}}, \code{\link[=qryflow_parse]{qryflow_parse()}}
}
